const autenticado = true;
const puedepagar = true;

console.log(autenticado ? "si está autenticado" : null);

console.log(autenticado&&puedepagar?"Si puede pagar":"no puede krnal");


console.log(autenticado? puedepagar ? "Autenticado and pay" : "Autenticado but not pay" :"no puede krnal");