const producto = {
    nombre:"Monitor 20 pulgadas",
    precio:300,
    disponible:true
}

producto.disponible = false;

console.log(producto);

//Aunque el onjeto es const, sus propiedades si se pueden editar