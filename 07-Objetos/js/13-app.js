const producto = {
    nombre:"Monitor 20 pulgadas",
    precio:300,
    disponible:true,
}

//keys
console.log( Object.keys(producto) ); //Retorna propiedades

//values
console.log( Object.values(producto) ); //retorna datos

//entries
console.log( Object.entries(producto) ); //retorna ambos, datos y propiedades